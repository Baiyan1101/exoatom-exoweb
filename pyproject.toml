[project]
name = "exoatom"
version = "0.1.0"
description = "The ExoAtom spectral database"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyvalem>=2.8.0",
    "wagtail>=6.4.1",
]

[dependency-groups]
dev = [
    "ipython>=9.1.0",
]
